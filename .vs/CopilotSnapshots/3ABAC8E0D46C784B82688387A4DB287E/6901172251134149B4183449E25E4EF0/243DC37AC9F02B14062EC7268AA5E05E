name: .NET Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.0.x'
    - name: Restore
      run: dotnet restore src/CyberAwarenessBot
    - name: Build
      run: dotnet build --no-restore src/CyberAwarenessBot
    - name: Run tests
      run: |
        dotnet test --no-build --verbosity normal
